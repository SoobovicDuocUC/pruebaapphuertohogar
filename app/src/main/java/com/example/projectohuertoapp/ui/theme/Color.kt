package com.example.projectohuertoapp.ui.theme

import androidx.compose.ui.graphics.Color

// --- Colores extraídos de la web HuertoHogar ---

// Verde Principal (botones, acentos, header en la app)
val VerdePrincipalWeb = Color(0xFF2E8B57)
// Verde Oscuro (para variantes o estados como hover/pressed)
val VerdeOscuroWeb = Color(0xFF246A44)
// Fondo principal claro de la web y app
val FondoClaroWeb = Color(0xFFF7F7F7)
// Texto principal oscuro
val TextoPrincipalWeb = Color(0xFF333333)
// Texto secundario o gris medio
val TextoSecundarioWeb = Color(0xFF666666)
// Blanco estándar
val BlancoWeb = Color.White
// Rojo para errores (estándar)
val RojoErrorWeb = Color(0xFFD32F2F)

// --- Colores para el modo oscuro (deducidos/estándar) ---
val FondoOscuroApp = Color(0xFF121212) // Fondo oscuro común en apps
val SuperficieOscuraApp = Color(0xFF1E1E1E) // Para Cards en modo oscuro
val TextoClaroSobreOscuro = Color(0xFFE0E0E0) // Texto principal en modo oscuro
val TextoGrisSobreOscuro = Color(0xFFB0B0B0)  // Texto secundario en modo oscuro
